<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unikom.tugasutsakb.fauzisunarya_10117110">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/smiling"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/smiling"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.FriendAddUpdateActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".view.SplashScreenActivity"></activity>
        <activity android:name=".view.OnBoardSliderActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>